# Copy to .env and fill in. .env is gitignored — never commit a real key.
#
# WARNING: anything in here is inlined into the JS bundle at build time and can
# be extracted from a shipped .ipa/.apk. Use a throwaway development key, and
# route production traffic through your own backend. See src/services/sleepSummary.ts.
ANTHROPIC_API_KEY=
